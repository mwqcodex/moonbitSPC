# moonbitSPC — MoonBit 制造质量统计与过程控制库

## 项目目标与场景

机械加工、实验室检测和小型产线常有测量数据，却缺少轻量、可嵌入且可复核的统计过程控制内核。`moonbitSPC` 面向质量工程师、设备软件开发者和教学人员，将批次、设备、刀具、操作员与时间窗口等追溯信息和测量值统一为确定性结果，降低从数据到质量判断的实现成本。

## 交付范围

首期交付纯 MoonBit 库：X-bar/R、X-bar/S、I-MR、P、NP、C、U 控制图数据；均值、样本标准差、Cp/Cpk、Pp/Ppk、超差率；Western Electric 四条异常规则；以及可序列化的质量报告。输入错误通过结果枚举返回，报告只提供结构化数据，不绑定数据库、绘图库或工厂协议。仓库提供 README、API 示例、测试、许可证、开发历程和跨平台 CI。

## 技术路线

项目按统计摘要、控制图、规则检测、能力分析、报告组装分层，使用 MoonBit 的代数数据类型和类型推断表达“成功/输入错误”边界；核心仅依赖标准运行时，目标为 wasm-gc 与 native。控制图计算使用公开的经典常数和明确的子组假设，测试覆盖正常值、边界值、变尺寸暴露量、空输入与零方差。AI 工具只用于检索语法、生成边界案例候选和整理检查表，公式、API、测试预期与取舍由作者复核。

## 风险与后续

样本分布、控制限估计和多变量相关性会影响实际判定；首期通过文档写明假设并返回原始控制点，后续增加基线窗口、漂移检测、多变量 SPC、刀具寿命预测、CSV/JSON 适配器和实时流式接口。该边界保证首期可构建、可测试、可复现，同时为质量平台和教学实验保留扩展点。

## 研究与生态价值

已在 Mooncakes 公开目录及公开检索结果中核查，未发现与制造 SPC 控制图、过程能力和质量追溯高度重合的成熟 MoonBit 包；项目因此选择生态中较空缺、但领域知识明确且可持续扩展的基础库方向。完成后将以唯一作者身份维护 GitHub 与 Gitlink 仓库，保留可审计的开发历史，并持续补充真实样本验证。
