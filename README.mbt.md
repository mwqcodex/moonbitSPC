# moonbitSPC

The checked API examples live in the package tests; use [README.md](README.md) for installation, architecture, assumptions, and CLI usage.

```mbt nocheck
///|
let chart = @moonbitSPC.ControlChart::imr([1.0, 1.1, 0.9, 1.0])
```
